@model IEnumerable<Document>
@if (Model.Count() > 0)
{
    <hr />
    <h4>Tillhörande document</h4>
    <ul>
        @foreach (Document Doc in Model)
        {
            <li>@Doc.Name</li>
        }
    </ul>
}