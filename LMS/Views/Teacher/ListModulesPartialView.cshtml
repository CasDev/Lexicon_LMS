@model IEnumerable<Module>
@if (Model.Count() > 0)
{
    <hr />
    <h5>Relaterade moduler:</h5>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndDate)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <a href="@Url.Content("~/Teacher/Module/"+ item.Id)" class="none">@item.Name</a>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.StartDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EndDate)
                </td>
                @if (item.EndDate > DateTime.Now)
                {
                    <td>
                        @Html.ActionLink("Redigera", "EditModule", new { id = item.Id })
                        |
                        @Html.ActionLink("Information", "Module", new { id = item.Id })
                    </td>
                }
                else
                {
                    <td></td>
                }
            </tr>
        }

    </table>
}