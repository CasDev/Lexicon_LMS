@model Course
@{
    ViewBag.Title = "Old Modules";
}

<h2>Tidigare moduler för @Model.Name</h2>
<p>Kursen pågick mellan den <time datetime="@Model.StartDate.ToString("yyyy-MM-dd")">@Model.StartDate.ToString(" yyyy-MM-dd ")</time> och den <time datetime="@Model.EndDate.ToString("yyyy-MM-dd")">@Model.EndDate.ToString(" yyyy-MM-dd ")</time></p>
@Html.Partial("ModulesTable", Model.Modules)
